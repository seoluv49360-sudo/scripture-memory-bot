services:
  scripture-memory-bot:
    build: .
    container_name: scripture-memory-bot
    restart: unless-stopped
    env_file:
      - .env
    dns:
      - 1.1.1.1
      - 8.8.8.8
    volumes:
      - ./data:/app/data
