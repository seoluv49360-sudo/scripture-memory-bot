services:
  scripture-memory-bot:
    build: .
    container_name: scripture-memory-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./reminders.json:/app/reminders.json
